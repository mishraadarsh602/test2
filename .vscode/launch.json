{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Gen AI Backend",
            "program": "${workspaceFolder}/server.js",
            "cwd": "${workspaceFolder}",
        }
    ]
}